import { NgModule } from 'ng-metadata/core';
import { ActivityNotificationCountDirective } from './notification-count.directive';

@NgModule({
	declarations: [
		ActivityNotificationCountDirective,
	],
})
export class ActivityNotificationCountModule { }
