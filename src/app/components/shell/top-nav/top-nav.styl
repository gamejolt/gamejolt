@require '~styles/variables'

#shell-top-nav
	position: fixed
	top: 0
	left: 0
	right: 0
	z-index: $zindex-shell-top-nav

	#shell.has-banner &
		top: $shell-top-nav-height

	.disconnected-icon
		color: var(--user-pink)

		&, .jolticon
			cursor: help
