.score-overview-user-best
	.well, .alert
		@extend .full-bleed-xs
		position: relative
		margin-bottom: 0

	@media $media-sm-up
		.alert::before
			caret( $gj-pink, size: 9px )
			content: ''

		.well::before
			caret( $gray-dark, size: 9px )
			content: ''
