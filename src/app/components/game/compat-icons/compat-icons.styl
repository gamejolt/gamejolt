@require '~styles/variables'

.game-compat-icons

	.jolticon
		position: relative
		top: 1px  // Bump it down to match up with the bottom of the text more.
		color: $gray-lighter

		.fill-dark &
		.fill-darker &
		.fill-darkest &
			color: $gray-light
