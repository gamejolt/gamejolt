@require '~styles/variables'
@require '~styles-lib/mixins'

.manage-game-nav-required
	@media $media-md
		position: absolute
		right: -25px

	@media $media-lg
		position: absolute
		right: 0

.incomplete
	.jolticon
		theme-prop('color', 'fg-muted', true)

.complete
	.jolticon
		theme-prop('color', 'link', true)
