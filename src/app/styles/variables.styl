// Set up our global variables.
@require 'game-jolt-frontend-lib/stylus/common/variables'
@require './layers'

/**
 * Shell
 */
$shell-border-color = $black
$shell-pane-width = 280px
$shell-top-nav-height = 50px
$shell-user-box-height = 125px
$shell-cbar-width = 70px
$status-bar-height = 28px
// Have to override $body-bg from bootstrap to be transparent.
// It then falls through to what `html` is set to.
$body-bg = transparent
$shell-body-content-bg = $white
/**
 * Chat
 */
$chat-bubble-size = 60px
$chat-room-window-width = 1000px
$chat-room-window-padding = 10px
/**
 * Forums
 */
$forum-post-list-icon-size = 50px
$forum-post-list-avatar-margin = $grid-gutter-width
/**
 * Minbar
 */
$minbar-offset-right = $chat-bubble-size
/**
 * Comment Video Thumbnail
 */
$comment-video-thumbnail-avatar-size = 35px
$comment-video-thumbnail-meta-spacing = 7px
/**
 * Modals
 */
$modal-padding = ($grid-gutter-width / 2)
$modal-padding-xs = ($grid-gutter-width-xs / 2)
/**
 * Page Header
 */
$page-header-spotlight-size = 100px
$page-header-spotlight-margin = ($grid-gutter-width / 2)
/**
 * Cbar
 */
$cbar-h-padding = 10px
