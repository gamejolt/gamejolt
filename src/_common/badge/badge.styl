.badge
	change-bg('bg-offset')
	theme-prop('color', 'fg')
	display: inline-block
	padding: 4px 7px
	font-size: $font-size-tiny
	font-weight: bold
	line-height: 1
	vertical-align: baseline
	white-space: nowrap
	text-align: center
	// Just some big number to round it.
	border-radius: 50px

	&:empty
		display: none

	a&
		pressy()

.badge-subtle
	change-bg('bg-subtle')

.badge-highlight
	change-bg('highlight')
	theme-prop('color', 'highlight-fg')

.badge-notice
	change-bg('notice')
	theme-prop('color', 'notice-fg')

.badge-overlay-notice
	change-bg-hex($gj-overlay-notice, 1)
	color: white

.badge.warning
	@extend .badge-notice
