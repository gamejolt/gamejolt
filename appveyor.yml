platform:
  - x86

install:
  - ps: Install-Product node 5.5.0 x86 # We need npm@3 for windows builds
  - npm install -g gulp bower node-pre-gyp nw-gyp
  - git submodule update --init --recursive
  - choco install -y InnoSetup
  - set PATH="C:\Program Files (x86)\Inno Setup 5";%PATH%

build_script:
  - bower i
  - npm i
  - npm run client-build-win

artifacts:
  - path: build\client\prod\build\win32\Setup.exe
  - path: build\client\prod\build\win32\win32-package.tar.gz
